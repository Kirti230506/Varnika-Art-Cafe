# --- DATABASE CONNECTION SETTINGS ---
# Defaults for Docker Compose network; can be overridden by environment variables
spring.datasource.url=jdbc:mysql://artcafe-mysql:3306/artcafe_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=artcafe
spring.datasource.password=artcafe123

# --- JPA / HIBERNATE CONFIGURATION ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- SERVER SETTINGS ---
server.port=8080
spring.application.name=ArtCafe
